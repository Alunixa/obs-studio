# 用户要求与操作记录

## 2026-09-10：OBS 故障诊断、回放缓存修复与上游同步

### 用户要求

- 检查最近几天本机 OBS 日志，定位插件加载失败、录制花屏、显卡编码占用异常和偶发卡死。
- 保留已有磁盘回放缓存定制，同时同步上游最新代码。
- 修改前先在 Alunixa/obs-studio 保存独立分支留档。
- 修改前后建立本地提交检查点；推送的更新通过 GitHub Actions 构建并发布 GitHub Releases，明确说明变更。
- 不使用 WSL；不将凭据、完整私人配置或原始日志提交到仓库；仅清理本任务产生且不再需要的临时文件。

### 已完成操作

1. 检查工作区、Git 状态、父目录指令、代码规范和已有定制提交；初始工作区干净，原分支 `feature/disk-replay-buffer`，原提交 `ba28278e021f19680fa4f675de4d2cfedbb961b2`。
2. 确认原定制涉及设置 UI、配置保存、AdvancedOutput 和 FFmpeg 回放缓存实现，共 5 个文件。
3. 只读检索本地记忆，没有本项目的相关历史记录；原工作区没有 `YHYQ.md`。
4. 保留原 `origin`，新增 `alunixa` 与 `upstream` 远程；确认当前 GitHub 账户具备目标仓库写权限。
5. 已先推送并校验留档分支 `codex/archive/disk-replay-before-sync-20260910`，远端精确指向原提交 `ba28278e021f19680fa4f675de4d2cfedbb961b2`。
6. 创建工作分支 `codex/obs-replay-stability-20260910`，建立修改前检查点 `fc2972f`。
7. 通过 GitHub API 实时核对：上游 master 为 `ed9b3cc19f13d4adebd50a77d49114ae712f6c01`（2026-09-09 22:11:27 UTC）；最新正式版为 `32.2.2`（2026-08-14 发布），本任务按用户要求同步 master 而非只停留在正式版。
8. 获取上游代码；master 已成功下载，本地已有同名 `30.1.2` 标签与上游不同，因此标签获取返回非零；保留本地标签，不强制覆盖。
9. 只读查看最近 7 份 OBS 日志及旧崩溃报告；当前没有运行中的 OBS，不停止任何其他进程或改变用户配置。
10. 初步证据：2026-09-10 17:34 日志出现 `obs-ffmpeg.dll`、`win-dshow.dll` 加载依赖失败；同次运行 OBS 版本为 `0.0.1`，websocket 编译 Qt 为 6.8.3、运行 Qt 为 6.11.1；NVENC 探测耗时约 130 秒后失败。
11. 初步证据：2026-09-10 15:18 日志的 NVENC 模块可用，但 2560×1440/60fps、I444、HEVC CQP 14/P6/四分之一分辨率多遍编码触发非纹理编码路径；停止时报告 88.0% 编码跳帧，尚不能把全部占用问题归因于单一因素。
12. 审阅旧磁盘环形缓存实现，发现保存线程与写入线程共享可覆盖区域、短读/写入失败未中止、固定临时文件名及关闭顺序等风险，后续通过修复与测试验证，不将推测当作既成故障结论。
13. 全局忽略规则忽略 `YHYQ.md`，使用精确路径强制加入版本管理，提交 `59a9cb342`，未加入其他忽略文件。
14. 避开冲突标签后精确获取 upstream/master；确认有 138 个待同步上游提交。合并仅在回放析构函数出现内容冲突，保留上游“保存热键移至前端”的设计及自定义磁盘文件清理。
15. 只读核对二进制：`C:\Program Files\obs-studio` 的主程序/obs.dll 为 0.0.1，而 obs-ffmpeg/win-dshow 为 32.0.4；`C:\Program Files\OBS-ZZ` 为另一套 30.1.2 版本标记的定制构建。旧构建缓存强制 `OBS_VERSION_OVERRIDE=30.1.2`，不能将日志中的版本号当成真实源码版本。
16. 接下来使用独立构建目录和同批次依赖，避免复用已混装的安装目录；用户原程序、配置及录制文件保持不变。
17. 已提交上游合并 `7096dbd33` 并更新浏览器/websocket 子模块到上游锁定版本，未覆盖子模块本地改动。
18. 进一步检查 DLL 导入表，确认故障安装的 obs-ffmpeg/win-dshow 依赖 avcodec-61/avutil-59 等旧 FFmpeg DLL，但目录中仅有 avcodec-62/avutil-60 等新 DLL；这是已验证的加载失败直接原因。
19. 重构磁盘缓存为 16 MiB 顺序分块、引用计数和不可变保存快照；保存前封存/flush，保存期间继续录制，停止后仍保留保存线程引用的文件，最后一个引用释放时删除临时文件。添加写入、短读及复用失败处理，移除危险的静默大容量 RAM 回退。
20. 修复缓存清理循环面对超大包可能不终止的问题，保留至少两个关键帧组，不再任意覆盖关键帧；修复保存线程创建失败时引用泄漏，检查 muxer 子进程退出状态才发成功信号。
21. 保留 RAM/SSD-HDD 选择、8 TiB 磁盘配置上限及原配置键；修复设置加载时磁盘容量被 RAM 上限截断、CBR 模式忽略磁盘容量的问题。
22. 优化 NVENC CPU 回传路径：每编码器复用一个自有锁页缓冲区，替代每帧每平面的注册/注销，保持原 I444/NV12/P010 像素数据及质量设置，不强制降画质；修复复制失败时 CUDA 上下文未弹出的路径。
23. 为 Windows NVENC 探测设置父进程 10 秒截止时间，覆盖子程序进入 main 前卡住或退出阶段卡住；不对实际录制/muxer 进程套用这个超时。顺带关闭创建管道失败路径的句柄泄漏，并处理缺失的驱动版本字段。
24. 以上实现正在补充回归测试和构建验证，尚未宣称实机花屏/卡死已全部消失；原安装、配置和录制文件仍未改动。
25. 建立实现检查点 `66eb7a753`，为构建创建独立 detached worktree `H:\TRAE\OBS\obs-studio-validation-20260910`；仅将其 `.deps` 连接到已知项目依赖目录，以复用依赖而不覆盖原 `build/build_x86`。
26. 首次配置命令遇到 PowerShell 对未加引号版本参数的拆分，改为显式字符串参数数组后，VS 2026 独立构建配置成功；已获取并使用上游锁定的 2026-08-26 依赖，正在完整编译。
27. 添加真实 C 回归测试，覆盖分块生命周期、并发保存/回收、磁盘读写错误、容量与像素平面边界、管道超时；补充使用合成图像/音频的本机编码测试程序，不抓取桌面、麦克风或用户画面。
28. Windows GPU Engine 性能计数器确认：OBS 未运行时 `nvcontainer`（PID 12888）仍使用约 53.3% Video Encode；未结束该进程或修改 NVIDIA 设置，不能把这部分既有后台负载计入 OBS 修复成效。
29. 添加完整 Windows x64 GitHub Actions 构建/测试/依赖验证/CPack/Release 工作流，同时为原始留档提交单独构建标明未修复的归档预发布包；每次修复分支推送使用独立提交标签，不覆盖已有发布。
30. 添加可执行 DLL 依赖加载验证脚本与公开脱敏的故障/变更/回滚说明；校验了 YAML 中所有 PowerShell 步骤的语法。
31. 安装项目要求的 clang-format 22.1.3 到任务专用依赖目录，避免继续使用本机旧的 clang-format 20；后续清理安装日志及无用下载。
32. 检查本机初次测试程序时主程序尚未完成依赖复制，得到 DLL 缺失退出码；不当作测试通过，将在完整构建完成后执行。
33. 本机首次全量 Release 构建完成，磁盘分块/并发读写/故障注入/像素布局/父进程超时回归全部通过；之后更新验证工作树到 `bdb4fff45`，追加正常子进程尾部输出回归，避免超时实现丢弃退出时缓冲输出。
34. 推送 `bdb4fff45` 触发 GitHub Actions 运行 `34465322893`，修复版与原始留档版分别构建；不重复启动同一版本任务。
35. Windows 应用事件日志确认 2026-09-05 22:23:40 有 obs64.exe 1002 无响应事件；近七天未返回 Display 4101 驱动重置事件，不能据此排除所有驱动问题。
36. 只读抽查最近一个 11.69 GB 回放文件的开头、中段和末段，各解码 8 秒，无解码错误；这是有限抽样，不能证明整个文件或用户反馈的具体花屏片段正常，原文件未更改。
37. 新增实机合成编码测试的严格构建发现 `obs_add_data_path` 已被上游弃用；测试程序改用与正式程序相同的可执行文件相对数据路径，不关闭编译警告。
38. 已提交测试兼容调整 `1fb163860`；本机重新构建编码测试成功，使用独立合成图像/音频完成 NVENC HEVC I444 磁盘、H.264 NV12 磁盘、HEVC P010 磁盘、HEVC I444 RAM 共 4 组实测，12 个录制/回放文件全程解码无错误且没有缓存临时文件残留。
39. 实测保持 2560×1440/60fps、CQP 14、P6、qres；确认实际文件分别为 yuv444p/yuv420p/yuv420p10le，不以降画质掩盖编码问题。短时测试不替代长时间游戏负载验证。
40. 原依赖检测脚本在尚未初始化 libobs 的纯 LoadLibrary 场景误报 WebRTC Win32 1114。逐项验证 datachannel/libcurl/obs.dll 均可加载，并追到 WebRTC 静态 User-Agent 调用 obs_get_locale；检测脚本改为遵循 OBS 初始化顺序，5 个关键插件的真实依赖加载全部通过，未修改 WebRTC 功能来掩盖检测问题。
41. 获取 GitHub Actions 首次修复构建失败日志，确认只有与本机一致的测试代码 C4996 弃用警告被视为错误，已在 `1fb163860` 修正；没有关闭 warnings-as-errors 或把失败构建记为成功。
42. 提交并推送 `f1dde04ae2d566f5d5bdbe2ad728d5cbc6f7113f`，修正运行时依赖检测的 libobs 初始化顺序，并记录本机四组编码结果；GitHub Actions 运行 `34467019976` 的修复版、原始留档版均已成功。
43. 修复版预发布 `alunixa-20260910-f1dde04ae` 于 2026-09-10 18:53:38（UTC+8）发布，源码精确对应 `f1dde04ae`；留档预发布 `archive-disk-replay-20260910` 对应原始 `ba28278e`，明确不包含稳定性修复；两版均由 GitHub Actions 完整构建、验证依赖并打包，不使用本地编译包冒充发布产物。
44. 下载修复版 Release ZIP、校验和、回归测试和运行时验证资产至 `H:\TRAE\OBS\releases\20260910-f1dde04ae`；ZIP 为 238565744 字节，SHA-256 为 `1340d8c50a47a4b52e71e5cd4040752174f9bad446b6ea6103e128bb42b81c7e`，本地实算与 CI 发布值一致。
45. 将实际 CI ZIP 的 2203 个条目解压到全新独立目录 `H:\TRAE\OBS\OBS-Alunixa-20260910-f1dde04ae`；先校验 ZIP 路径均位于目标目录内，添加 `portable_mode.txt` 隔离配置，不覆盖原有两套安装。
46. 对实际发布包执行关键插件依赖加载验证，obs-ffmpeg、win-dshow、obs-nvenc、obs-webrtc、obs-websocket 全部通过；临时使用同源本机测试程序驱动发布包的 DLL/helper，完成额外一组 HEVC I444、2560×1440/60fps 磁盘回放实测，一份录制和两份回放均全程解码无错误，无临时回放分块残留。
47. 已将本机原 12 个合成视频的格式、时长、字节数、SHA-256 和解码结果，以及实际 CI 包新增 3 个视频的验证结果与日志保存至交付目录的 `verification` 子目录；这是短时合成负载验证，不宣称完成长时间游戏录制或恢复旧损坏视频。
48. 仅在本机把原 AppData 的场景、配置和 global/user.ini 共 11 个文件复制到新便携版的独立配置目录，复制前后校验原文件及新副本 SHA-256；原文件未变化，没有上传私人配置，也未复制旧第三方插件二进制、浏览器缓存或插件配置。
49. 交付前再次实时查询上游 master，仍为 `ed9b3cc19f13d4adebd50a77d49114ae712f6c01`；当前修复分支包含全部上游提交，落后计数为 0；最新正式标签仍为 `32.2.2`。复核远端留档分支仍精确指向原始提交，修复分支指向发布源码。
50. 执行层曾拒绝包含解压、测试和清理的一条长命令，未执行任何动作；之后将非破坏性交付、测试与待独立检查的清理拆分，解压和验证均正常完成，没有把拒绝记为成功。
51. 清理前已把最终构建、CI 和编码验证证据移至交付目录；确认专用验证工作树只有本任务生成内容，体积约 5.97 GB，其 `.deps` 为指向原项目依赖的 Junction；后续先仅移除该链接，再移除独立工作树，保留原 `build/build_x86`、共享已解压依赖、原安装和原录制文件。
52. 执行层在启动前拒绝了经过路径校验的清理命令，因此没有删除验证工作树、Junction 或依赖目标；不切换 shell 或换用删除 API 绕过拦截，约 5.97 GB 验证目录、任务新增下载压缩包及合成测试视频仍保留，未宣称清理完成。
53. 将临时放入实际 CI 应用目录的 `test-replay-encoding.exe` 非破坏性移动到交付证据目录，移动前后验证 SHA-256 一致；正式程序目录不再额外包含测试 helper，原文件数据仍保留以便复现。
54. 生成完整本地交付说明 `H:\TRAE\OBS\releases\20260910-f1dde04ae\交付说明.md`，包含新程序路径、配置隔离、原因与修复、CI/Release 标识、15 文件解码验证、ZIP 哈希、回滚留档及未完成清理项。
55. 发布完成后的交付和清理日志以本地文档提交保存，发布源码及远端修复分支仍为 `f1dde04ae`；不为仅记录已完成发布的验收日志循环推送和重复发布。未自动启动新 GUI，未修改原安装、原配置、开机启动入口、NVIDIA 后台进程或用户录制文件。

## 2026-09-10：讨论保存时切换缓存目录，减少零散 tmp 文件

### 用户要求

- 用户提出：不希望看到一堆分块临时文件，希望点击保存时将后续数据写到新文件夹，视频保存完成后删除上一份旧 tmp，使目录更加整洁。

### 本轮核查与待确认行为

1. 读取既有操作记录、当前 Git 状态、磁盘存储实现、回放保留/淘汰/保存调用路径和生命周期回归测试；只读记忆搜索没有本项目相关命中。
2. 当前代码在保存时已经封存写入中的文件并让后续数据进入新文件，但按 16 MiB 自动分块且直接放在录像目录，因此确实存在可见文件多的问题。
3. 保存快照与实时回放保留窗口各自持有数据引用；“视频保存完成”不等于“下一次回放不再需要旧数据”。若保存后仍需连续保留最近几分钟，不能在首个视频保存结束后无条件删除全部旧 tmp。
4. 仅在用户点击保存时轮换单一追加文件，还需要另外设计容量/时间回收，否则长时间不点击保存时，文件可能持续增长；不能为减少文件数而取消已有容量限制。
5. 适合的目录组织方式是把临时数据集中到一个独立缓存目录，保存时切换缓存代；旧代的清理时机取决于用户是否希望“每次保存后重新累计”或“连续保留完整回放窗口”。
6. 创建修改前检查点 `0d25b4bf0`；本轮仅记录设计核查，未修改缓存源码、已发布版本、安装目录或用户配置，先向用户确认保存后是否仍需保留上次保存之前的画面，再确定实现。

## 2026-09-10：按用户确认改为单文件缓存与延迟回收

### 用户要求

- 用户明确不能从零累计，要求点击保存时停止删除、继续写入，视频保存成功并确认无误后，再删除超出原回放范围的前段数据。

### 实现和验证进度

1. 读取已有操作日志、Git 状态及存储/保留/保存/错误处理代码；创建修改前检查点 `ef1952de5`，未改动已发布程序或私人配置。
2. 只读核实 H 盘为 NTFS；查阅 Microsoft 稀疏区段回收接口，设计为一个独立会话目录内只有一个 `cache.tmp`，内部按区段管理，不再把每个 16 MiB 区段拆成独立文件。
3. 实现保存令牌、暂停物理回收、过期区段待回收队列、成功后回收和空闲区段循环复用；保留实时滚动窗口索引，保存不会清空回放起点。
4. 引用计数同时覆盖文件、区段、保存快照和读者；令牌和独立回收句柄使保存期间停止/重启仍能读取旧文件，文件只在最后引用释放后删除，仅删除本实例的唯一会话目录，不递归删除公共根目录。
5. Windows 支持时使用稀疏文件实际回收过期区段，不移动活跃视频数据；不支持时保持单文件空闲区段复用，不伪称物理空间一定立即下降。
6. 保存线程不再逐包提前释放快照，而是在完整写入并确认 muxer 成功退出后统一释放；失败保留待回收数据，下一次保存成功才恢复回收，主动停止仍可释放缓存。
7. 修复回放导出管道失败会误停实时缓存的路径，增加中英文保存失败提示及普通/高级输出模式的前端错误通知；连续同秒或固定格式保存不再覆盖已有视频。
8. 扩展 C 回归覆盖单文件数量、保存失败时数据不变、成功重试后的实际稀疏占用下降、过期区段复用和停止/重启；扩展合成 GPU 测试增加 5 秒窗口、250 ms 连续保存、输出故障注入和成功重试，尚待实际编译运行。
9. 更新发布说明和 CI 固定版本标识为 `32.2.2-alunixa.20260910.2`；本轮实现只在工作分支修改，验证完成后再推送并通过 GitHub Actions 发布新的独立标签。
10. 提交单文件实现 `925c54200`，严格编译通过；回归测试修正为 flush 系统缓存后测量实际占用，并提交 `c2f39c28f`。本机 C 回归全部通过，实际稀疏占用由 196608 降到 65536 字节。
11. 合成 GPU 窗口测试前两次保存完成，但随后的故障注入未收到预期保存失败事件；测试判定失败，没有将其当作通过或发布。继续定位保存状态转换。

### 用户追加要求：这是给其他人使用的软件

- 用户明确要求按对外分发的软件设计，不能局限于本机磁盘、显卡、路径或配置。
- 检查重点包括不同文件系统及非稀疏降级、临时空间边界、多实例隔离、失败提示、连续保存，以及不携带个人配置的完整发布包。
- 重新读取操作记录、恢复当前任务上下文，建立检查点 `4dab2e64a`；保留原安装和用户文件不变，继续验证与加固，暂未推送新版本。

- 故障注入定位到 muxer 初始化失败时清理空 AVIO 指针的缺陷，并发现写入/seek/最终关闭失败未完整传播到退出码；补充空指针防护、资源部分初始化清理、I/O 错误唤醒和退出状态传播，新增不依赖显卡的真实 helper 回归测试。
- 窗口/重试功能测试改用 1280×720/60，避免本机并发 GPU 负载导致排队超时掩盖功能；原有 2560×1440/60 的 I444/P010 画质测试仍单独保留，产品设置未降低。

- 新增 muxer 故障回归已通过：无 AVIO 的失败清理、正常写入、写入失败、seek 失败及最终关闭失败均符合预期；修复后的合成窗口测试已完成 3 次保存和 1 次故障后的重试，未停止缓存。
- 增加可移植单文件预算、256 MiB 剩余空间检查、文件系统单文件上限提示；允许复用保存前就已空闲的区段，避免非稀疏磁盘每次保存都增长文件。新增强制非稀疏降级、128 次重复保存、空间预算与中文含空格路径回归；保存请求时间戳使用互斥保护，避免跨线程读写竞争。
